version: '3.1'
services:
  nginx:
    restart: always
    build:
      dockerfile: Dockerfile.dev
      context: ./nginx
    ports:
      - '5000:80'
    depends_on:
      - api
      - client
  tamashii-mongo:
    image: mongo:latest
    container_name: 'tamashii-mongo'
    restart: always
    ports:
      - '27017:27017'
    logging:
      driver: none
  client:
    build:
      dockerfile: Dockerfile.dev
      context: ./client
    volumes:
      - /app/node_modules
      - ./client/:/app
      - /app/.next
  api:
    depends_on:
      - tamashii-mongo
    restart: always
    build:
      dockerfile: Dockerfile.dev
      context: ./server
    volumes:
      - /app/node_modules
      - ./server/:/app
    links:
      - tamashii-mongo
    environment:
      MONGO_HOST: tamashii-mongo
      MONGO_DATABASE: database
      MONGO_USERNAME: root
      MONGO_PASSWORD: tamashii